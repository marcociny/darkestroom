# Darkest Room
Adventure game made in ncurses.

# Installation

## Linux

Install the **gcc** and **ncurses** packages, then run the following command to compile the game's code: 

`g++ main.cpp -lncurses`

After that, run the executable file (named `a.out` by default).